export type CardProps = {
  className?: string;
  to?: string;
  tagLabel?: string;
  price: string;
  priceNote?: string;
  title: string;
  location: string;
  imageSrc?: string;
  showHeartButton?: boolean;
};
